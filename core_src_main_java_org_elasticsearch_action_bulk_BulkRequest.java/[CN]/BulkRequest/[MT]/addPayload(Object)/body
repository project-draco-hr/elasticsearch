{
  if (payloads == null) {
    if (payload == null) {
      return;
    }
    payloads=new ArrayList<>(requests.size() + 10);
    for (int i=1; i < requests.size(); i++) {
      payloads.add(null);
    }
  }
  payloads.add(payload);
}
