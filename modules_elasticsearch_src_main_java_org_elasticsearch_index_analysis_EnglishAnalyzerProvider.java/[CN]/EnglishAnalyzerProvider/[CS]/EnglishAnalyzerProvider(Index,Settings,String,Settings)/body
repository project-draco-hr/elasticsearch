{
  super(index,indexSettings,name,settings);
  analyzer=new EnglishAnalyzer(version,Analysis.parseStopWords(settings,EnglishAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
