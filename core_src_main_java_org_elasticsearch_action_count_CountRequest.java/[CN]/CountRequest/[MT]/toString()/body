{
  return "count request indices:" + Arrays.toString(indices) + ", types:"+ Arrays.toString(types)+ ", routing: "+ routing+ ", preference: "+ preference+ ", source:"+ searchSourceBuilder.toString();
}
