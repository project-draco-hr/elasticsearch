{
  if (obj == null) {
    return false;
  }
 else   if (!(obj instanceof Entry)) {
    return false;
  }
 else {
    @SuppressWarnings("unchecked") Entry<K,V> e=(Entry<K,V>)obj;
    return Objects.equals(key,e.key);
  }
}
