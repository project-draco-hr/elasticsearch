{
  builder.startObject(name);
  builder.startArray(Fields.TERMS);
  for (  Term term : terms) {
    term.toXContent(builder,params);
  }
  builder.endArray();
  builder.endObject();
  return builder;
}
