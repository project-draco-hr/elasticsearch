{
  if (version.size() >= 2 && version.get(0).intValue() == 1 && version.get(1).intValue() == 8) {
    version=new ArrayList<>(version.subList(1,version.size()));
  }
  this.version=Collections.unmodifiableList(version);
}
