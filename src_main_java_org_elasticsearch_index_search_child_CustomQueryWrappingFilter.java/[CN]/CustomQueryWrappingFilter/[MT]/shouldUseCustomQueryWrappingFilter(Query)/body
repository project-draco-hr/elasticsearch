{
  if (query instanceof TopChildrenQuery || query instanceof ChildrenConstantScoreQuery || query instanceof ChildrenQuery|| query instanceof ParentConstantScoreQuery|| query instanceof ParentQuery) {
    return true;
  }
 else {
    return false;
  }
}
