{
  super(index,indexSettings,name,settings);
  analyzer=new FinnishAnalyzer(version,Analysis.parseStopWords(settings,FinnishAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
