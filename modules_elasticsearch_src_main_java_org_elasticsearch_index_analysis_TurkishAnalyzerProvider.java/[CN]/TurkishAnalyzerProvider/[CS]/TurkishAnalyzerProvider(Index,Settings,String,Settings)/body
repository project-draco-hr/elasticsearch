{
  super(index,indexSettings,name,settings);
  analyzer=new TurkishAnalyzer(version,Analysis.parseStopWords(settings,ArabicAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
