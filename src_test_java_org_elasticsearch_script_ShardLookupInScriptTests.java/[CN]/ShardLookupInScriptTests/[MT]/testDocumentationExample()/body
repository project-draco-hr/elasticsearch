{
  initTestData();
  String script="termInfo = _shard['float_payload_field'].get('b'," + includeAllFlag + "); payloadSum=0; for (pos : termInfo) {payloadSum = pos.payloadAsInt(0);} return payloadSum;";
  HashMap<String,Object> zeroArray=new HashMap<String,Object>();
  zeroArray.put("1",0);
  zeroArray.put("2",0);
  zeroArray.put("3",0);
  checkValueInEachDoc(script,zeroArray,3);
  script="termInfo = _shard['int_payload_field'].get('b'," + includeAllFlag + "); payloadSum=0; for (pos : termInfo) {payloadSum = payloadSum + pos.payloadAsInt(0);} return payloadSum;";
  zeroArray.put("1",5);
  zeroArray.put("2",3);
  zeroArray.put("3",1);
  checkValueInEachDoc(script,zeroArray,3);
}
