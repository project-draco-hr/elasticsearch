{
  String script="termInfo = _shard['int_payload_field'].get('" + term + "',"+ flags+ "); array=[]; for (pos : termInfo) {array.add(pos."+ what+ ")} array=[]; for (pos : termInfo) {array.add(pos."+ what+ ")} return array;";
  return script;
}
