{
  super(index,indexSettings,name,settings);
  analyzer=new RomanianAnalyzer(version,Analysis.parseStopWords(settings,RomanianAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
