{
  byte[] facetSource=parser.binaryValue();
  XContentParser aSourceParser=XContentFactory.xContent(facetSource).createParser(facetSource);
  try {
    aSourceParser.nextToken();
    super.parse(aSourceParser,context);
  }
  finally {
    aSourceParser.close();
  }
}
