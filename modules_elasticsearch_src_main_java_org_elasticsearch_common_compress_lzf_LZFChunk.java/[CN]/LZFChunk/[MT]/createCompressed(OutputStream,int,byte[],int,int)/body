{
  os.write(BYTE_Z);
  os.write(BYTE_V);
  os.write(BLOCK_TYPE_COMPRESSED);
  os.write(encLen >> 8);
  os.write(encLen);
  os.write((origLen >> 8));
  os.write(origLen);
  os.write(encData,encPtr,encLen);
  return encLen + 7;
}
