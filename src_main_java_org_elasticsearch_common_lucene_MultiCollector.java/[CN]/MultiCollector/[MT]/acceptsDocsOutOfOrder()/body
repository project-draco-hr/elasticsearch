{
  if (!leafCollector.acceptsDocsOutOfOrder()) {
    return false;
  }
  for (  LeafCollector leafCollector : leafCollectors) {
    if (!leafCollector.acceptsDocsOutOfOrder()) {
      return false;
    }
  }
  return true;
}
