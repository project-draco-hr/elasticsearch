{
  return new MapperService(new Index("test"),ImmutableSettings.Builder.EMPTY_SETTINGS,new Environment(),newAnalysisService(),new PostingsFormatService(new Index("test")),newSimilarityLookupService());
}
