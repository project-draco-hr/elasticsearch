{
  String[] array=randomBoolean() ? new String[randomInt(arraySize)] : null;
  if (array != null) {
    for (int i=0; i < array.length; i++) {
      array[i]=randomAsciiOfLength(stringSize);
    }
  }
  return array;
}
