{
  if (scanContext != null) {
    scanContext.clear();
  }
  if (rewrites != null) {
    for (    Rewrite rewrite : rewrites) {
      rewrite.contextClear();
    }
  }
  searcher.release();
  engineSearcher.release();
  return true;
}
