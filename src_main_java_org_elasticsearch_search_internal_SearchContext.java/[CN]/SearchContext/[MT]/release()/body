{
  if (scanContext != null) {
    scanContext.clear();
  }
  if (scopePhases != null) {
    for (    ScopePhase scopePhase : scopePhases) {
      scopePhase.clear();
    }
  }
  engineSearcher.release();
  return true;
}
