{
  GeoStringFieldMapper fieldMapper=new GeoStringFieldMapper(buildNames(context),boost,fieldType,nullValue,indexAnalyzer,searchAnalyzer,provider);
  fieldMapper.includeInAll(includeInAll);
  return fieldMapper;
}
