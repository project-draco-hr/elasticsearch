{
  if (super.equals(obj) == false) {
    return false;
  }
  NestedChildrenFilter other=(NestedChildrenFilter)obj;
  return parentFilter.equals(other.parentFilter) && childFilter.equals(other.childFilter) && docId == other.docId && atomicReader.getCoreCacheKey() == other.atomicReader.getCoreCacheKey();
}
