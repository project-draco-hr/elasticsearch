{
  return NumericRangeFieldDataFilter.newLongRange(fieldDataCache,names.indexName(),lowerTerm == null ? null : parseStringValue(lowerTerm),upperTerm == null ? null : includeUpper ? parseUpperInclusiveStringValue(upperTerm) : parseStringValue(upperTerm),includeLower,includeUpper);
}
