{
  AnalyzeResponse response=client().admin().indices().prepareAnalyze("?????????").setAnalyzer("smartcn").execute().get();
  assertThat(response,notNullValue());
  assertThat(response.getTokens().size(),is(2));
}
