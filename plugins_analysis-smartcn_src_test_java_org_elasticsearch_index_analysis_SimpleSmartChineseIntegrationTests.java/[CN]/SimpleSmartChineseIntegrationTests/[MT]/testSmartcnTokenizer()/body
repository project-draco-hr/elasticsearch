{
  AnalyzeResponse response=client().admin().indices().prepareAnalyze("?????????").setTokenizer("smartcn_tokenizer").execute().get();
  assertThat(response,notNullValue());
  assertThat(response.getTokens().size(),is(2));
}
