{
  super(index,indexSettings,name,settings);
  analyzer=new ArmenianAnalyzer(version,Analysis.parseStopWords(settings,ArmenianAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
