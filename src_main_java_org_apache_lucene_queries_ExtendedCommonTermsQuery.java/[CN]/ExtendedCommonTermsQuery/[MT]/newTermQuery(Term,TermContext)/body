{
  if (mapper == null) {
    return super.newTermQuery(term,context);
  }
  final Query query=mapper.queryStringTermQuery(term);
  if (query == null) {
    return super.newTermQuery(term,context);
  }
 else {
    return query;
  }
}
