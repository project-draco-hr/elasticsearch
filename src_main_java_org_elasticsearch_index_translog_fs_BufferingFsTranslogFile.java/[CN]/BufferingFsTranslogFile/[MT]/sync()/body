{
  if (!syncNeeded()) {
    return;
  }
  rwl.writeLock().lock();
  try {
    flushBuffer();
    lastSyncPosition=lastPosition;
  }
  finally {
    rwl.writeLock().unlock();
  }
  channelReference.channel().force(false);
}
