{
  my_goto:   while (true) {
    for (    Client client : clients()) {
      TypesExistsResponse existsResponse=client.admin().indices().prepareTypesExists(index).setTypes("_percolator").execute().actionGet();
      if (existsResponse.isExists()) {
        continue my_goto;
      }
    }
    break;
  }
}
