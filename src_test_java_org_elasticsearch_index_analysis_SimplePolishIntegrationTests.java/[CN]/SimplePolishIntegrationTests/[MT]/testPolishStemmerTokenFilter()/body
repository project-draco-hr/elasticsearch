{
  AnalyzeResponse response=client().admin().indices().prepareAnalyze("canona").setTokenFilters("polish_stem").execute().get();
  assertThat(response,notNullValue());
  assertThat(response.getTokens().size(),is(1));
}
