{
  setupFieldType(context);
  DateFieldMapper fieldMapper=new DateFieldMapper(fieldType,docValues,nullValue,ignoreMalformed(context),coerce(context),fieldDataSettings,context.indexSettings(),multiFieldsBuilder.build(this,context),copyTo);
  fieldMapper.includeInAll(includeInAll);
  return fieldMapper;
}
