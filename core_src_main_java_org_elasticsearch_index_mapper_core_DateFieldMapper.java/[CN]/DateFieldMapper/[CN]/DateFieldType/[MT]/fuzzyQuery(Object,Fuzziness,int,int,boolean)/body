{
  long iValue=parseValue(value);
  long iSim;
  try {
    iSim=fuzziness.asTimeValue().millis();
  }
 catch (  Exception e) {
    iSim=fuzziness.asLong();
  }
  return NumericRangeQuery.newLongRange(name(),numericPrecisionStep(),iValue - iSim,iValue + iSim,true,true);
}
