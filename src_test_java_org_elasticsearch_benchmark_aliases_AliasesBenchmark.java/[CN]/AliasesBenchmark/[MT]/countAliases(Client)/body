{
  try {
    GetAliasesResponse response=client.admin().indices().prepareGetAliases("a*").addIndices(INDEX_NAME).execute().actionGet();
    return response.getAliases().get(INDEX_NAME).size();
  }
 catch (  AliasMissingException e) {
    return 0;
  }
}
