{
  return new Iterator<Byte>(){
    private final TByteIterator it=_set.iterator();
    public Byte next(){
      return wrap(it.next());
    }
    public boolean hasNext(){
      return it.hasNext();
    }
    public void remove(){
      it.remove();
    }
  }
;
}
