{
  if (name != null) {
    xcontent.startArray(name);
  }
 else {
    xcontent.startArray();
  }
  ring.emdedXContent(null,xcontent);
  for (  LinearRingBuilder<?> ring : holes) {
    ring.emdedXContent(null,xcontent);
  }
  xcontent.endArray();
}
