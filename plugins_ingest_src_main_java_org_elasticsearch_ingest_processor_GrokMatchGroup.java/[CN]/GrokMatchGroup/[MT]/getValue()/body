{
  if (groupValue == null) {
    return null;
  }
switch (type) {
case "int":
    return Integer.parseInt(groupValue);
case "float":
  return Float.parseFloat(groupValue);
default :
return groupValue;
}
}
