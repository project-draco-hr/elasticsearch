{
  if (randomBoolean()) {
    return new IndexWarmersMetaData(new IndexWarmersMetaData.Entry(randomName("warm"),new String[]{randomName("type")},randomBoolean(),new BytesArray(randomAsciiOfLength(1000))));
  }
 else {
    return new IndexWarmersMetaData();
  }
}
