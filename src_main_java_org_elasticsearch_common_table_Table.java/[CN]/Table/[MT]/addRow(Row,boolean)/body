{
  ensureCapacity(row.size());
  byte curCol=0;
  for (  Cell cell : row.cells()) {
    Column col=cols.get(curCol);
    col.addCell(cell,header);
    curCol+=1;
  }
  numcols=curCol;
  height+=1;
  return this;
}
