{
synchronized (other) {
    if (other.context == null) {
      return;
    }
    if (context == null) {
      context=new ObjectObjectOpenHashMap<>(other.context);
    }
 else {
      context.putAll(other.context);
    }
  }
}
