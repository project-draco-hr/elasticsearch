{
synchronized (other) {
    if (other.context == null) {
      return;
    }
    if (context == null) {
      context=new ObjectObjectHashMap<>(other.context);
    }
 else {
      context.putAll(other.context);
    }
  }
}
