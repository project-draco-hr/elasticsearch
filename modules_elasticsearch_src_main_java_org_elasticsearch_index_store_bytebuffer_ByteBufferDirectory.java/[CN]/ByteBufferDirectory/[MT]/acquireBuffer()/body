{
  if (disableCache) {
    return createBuffer();
  }
  ByteBuffer byteBuffer=cache.poll();
  if (byteBuffer == null) {
    return createBuffer();
  }
  byteBuffer.position(0);
  return byteBuffer;
}
