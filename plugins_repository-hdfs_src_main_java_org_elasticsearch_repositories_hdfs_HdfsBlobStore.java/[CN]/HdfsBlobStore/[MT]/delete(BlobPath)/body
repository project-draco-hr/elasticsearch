{
  SecurityUtils.execute(ffs,new FsCallback<Void>(){
    @Override public Void doInHdfs(    FileSystem fs) throws IOException {
      fs.delete(translateToHdfsPath(path),true);
      return null;
    }
  }
);
}
