{
  if (refCount.decrementAndGet() <= 0) {
    try {
      raf.close();
    }
 catch (    IOException e) {
    }
 finally {
      if (delete) {
        IOUtils.deleteFilesIgnoringExceptions(file.toPath());
      }
    }
  }
}
