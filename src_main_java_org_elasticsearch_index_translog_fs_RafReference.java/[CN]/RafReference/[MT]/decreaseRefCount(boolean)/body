{
  if (refCount.decrementAndGet() <= 0) {
    try {
      raf.close();
    }
 catch (    IOException e) {
    }
 finally {
      if (delete) {
        FileSystemUtils.deleteFilesIgnoringExceptions(file.toPath());
      }
    }
  }
}
