{
  if (wasEvicted) {
    evictionsMetric.inc();
  }
  if (sizeInBytes != -1) {
    totalMetric.dec(sizeInBytes);
  }
 else   if (fieldData != null) {
    totalMetric.dec(fieldData.getMemorySizeInBytes());
  }
}
