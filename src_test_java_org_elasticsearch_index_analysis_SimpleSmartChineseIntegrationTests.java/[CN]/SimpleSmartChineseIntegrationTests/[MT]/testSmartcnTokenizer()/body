{
  AnalyzeResponse response=client().admin().indices().prepareAnalyze("?????????").setTokenizer("smartcn_sentence").execute().get();
  assertThat(response,notNullValue());
  assertThat(response.getTokens().size(),is(1));
}
