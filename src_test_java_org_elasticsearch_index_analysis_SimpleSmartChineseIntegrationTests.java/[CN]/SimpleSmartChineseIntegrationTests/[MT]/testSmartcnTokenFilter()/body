{
  AnalyzeResponse response=client().admin().indices().prepareAnalyze("?????????").setTokenFilters("smartcn_word").execute().get();
  assertThat(response,notNullValue());
  assertThat(response.getTokens().size(),is(3));
}
