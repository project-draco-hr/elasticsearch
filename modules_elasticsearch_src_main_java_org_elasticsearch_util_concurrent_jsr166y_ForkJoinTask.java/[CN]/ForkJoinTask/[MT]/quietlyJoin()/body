{
  Thread t=Thread.currentThread();
  if (t instanceof ForkJoinWorkerThread) {
    ForkJoinWorkerThread w=(ForkJoinWorkerThread)t;
    if (!w.unpushTask(this) || !tryExec())     awaitDone(w);
  }
 else   externalAwaitDone();
}
