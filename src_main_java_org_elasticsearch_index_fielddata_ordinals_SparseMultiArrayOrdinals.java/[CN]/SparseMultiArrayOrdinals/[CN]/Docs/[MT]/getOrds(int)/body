{
  spare.end=0;
  int pointer=lookup[docId];
  if (pointer == 0) {
    return IntArrayRef.EMPTY;
  }
 else   if (pointer > 0) {
    spare.end=1;
    spare.values[0]=pointer;
    return spare;
  }
 else {
    pool.fill(spare,-(pointer + 1));
    return spare;
  }
}
