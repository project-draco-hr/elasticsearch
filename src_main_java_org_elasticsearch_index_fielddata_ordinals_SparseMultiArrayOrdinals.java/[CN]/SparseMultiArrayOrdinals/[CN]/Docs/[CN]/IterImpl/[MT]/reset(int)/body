{
  final int pointer=lookup[docId];
  if (pointer < 0) {
    pool.fill(slice,-(pointer + 1));
  }
 else {
    slice.values[0]=pointer;
    slice.start=0;
    slice.end=1;
  }
  valuesOffset=0;
  return this;
}
