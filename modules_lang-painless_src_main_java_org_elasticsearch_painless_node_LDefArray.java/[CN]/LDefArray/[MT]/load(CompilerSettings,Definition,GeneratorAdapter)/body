{
  final String desc=Type.getMethodDescriptor(after.type,definition.defType.type,index.actual.type);
  adapter.visitInvokeDynamicInsn("arrayLoad",desc,DEF_BOOTSTRAP_HANDLE,new Object[]{DefBootstrap.ARRAY_LOAD});
}
