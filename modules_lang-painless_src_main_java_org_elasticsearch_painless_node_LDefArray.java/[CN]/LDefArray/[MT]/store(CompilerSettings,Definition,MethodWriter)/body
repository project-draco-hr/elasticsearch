{
  final String desc=Type.getMethodDescriptor(definition.getType("void").type,definition.getType("def").type,index.actual.type,after.type);
  adapter.invokeDynamic("arrayStore",desc,DEF_BOOTSTRAP_HANDLE,DefBootstrap.ARRAY_STORE);
}
