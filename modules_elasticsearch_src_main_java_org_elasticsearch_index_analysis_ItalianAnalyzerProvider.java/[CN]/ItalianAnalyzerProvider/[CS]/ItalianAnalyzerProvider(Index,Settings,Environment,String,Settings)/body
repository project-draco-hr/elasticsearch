{
  super(index,indexSettings,name,settings);
  analyzer=new ItalianAnalyzer(version,Analysis.parseStopWords(env,settings,ItalianAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
