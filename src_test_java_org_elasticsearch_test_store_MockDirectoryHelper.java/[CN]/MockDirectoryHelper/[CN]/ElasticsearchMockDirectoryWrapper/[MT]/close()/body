{
  try {
    super.close();
  }
 catch (  RuntimeException ex) {
    logger.info("MockDirectoryWrapper#close() threw exception",ex);
    closeException=ex;
    throw ex;
  }
 finally {
synchronized (lock) {
      lock.notifyAll();
    }
  }
}
