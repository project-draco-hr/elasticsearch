{
  if (len == 0) {
    return;
  }
  if (pos >= end) {
    throw new EOFException();
  }
  if (pos + len > end) {
    len=end - pos;
  }
  if (len <= 0) {
    throw new EOFException();
  }
  System.arraycopy(buf,pos,b,offset,len);
  pos+=len;
}
