{
  return execute(request,new TaskListener<Response>(){
    @Override public void onResponse(    Task task,    Response response){
      listener.onResponse(response);
    }
    @Override public void onFailure(    Task task,    Throwable e){
      listener.onFailure(e);
    }
  }
);
}
