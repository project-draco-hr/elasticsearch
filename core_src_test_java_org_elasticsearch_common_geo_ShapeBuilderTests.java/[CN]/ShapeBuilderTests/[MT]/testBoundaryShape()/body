{
  PolygonBuilder builder=ShapeBuilders.newPolygon(new PointListBuilder().point(-180,90).point(180,90).point(180,-90).point(-180,90).list());
  Shape shape=builder.close().build();
  assertPolygon(shape);
}
