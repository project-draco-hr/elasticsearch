{
  if (version.onOrAfter(Version.LUCENE_48)) {
    return new WordDelimiterFilter(version,tokenStream,charTypeTable,flags,protoWords);
  }
 else {
    return new Lucene47WordDelimiterFilter(tokenStream,charTypeTable,flags,protoWords);
  }
}
