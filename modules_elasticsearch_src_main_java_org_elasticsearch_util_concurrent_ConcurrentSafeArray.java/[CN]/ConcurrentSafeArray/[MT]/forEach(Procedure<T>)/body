{
  rwl.readLock().lock();
  try {
    for (int i=0; i < list.size(); i++) {
      if (!procedure.execute(list.get(i))) {
        return false;
      }
    }
    return true;
  }
  finally {
    rwl.readLock().unlock();
  }
}
