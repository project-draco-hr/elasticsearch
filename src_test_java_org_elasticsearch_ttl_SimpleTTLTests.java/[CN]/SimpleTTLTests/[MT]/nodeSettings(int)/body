{
  return settingsBuilder().put(super.nodeSettings(nodeOrdinal)).put("indices.ttl.interval",PURGE_INTERVAL,TimeUnit.MILLISECONDS).put("cluster.routing.operation.use_type",false).put("cluster.routing.operation.hash.type","djb").build();
}
