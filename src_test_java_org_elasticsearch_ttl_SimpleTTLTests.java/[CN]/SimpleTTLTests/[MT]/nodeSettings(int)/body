{
  return settingsBuilder().put(super.nodeSettings(nodeOrdinal)).put("indices.ttl.interval",PURGE_INTERVAL).put("index.number_of_shards",2).put("cluster.routing.operation.use_type",false).put("cluster.routing.operation.hash.type","djb").build();
}
