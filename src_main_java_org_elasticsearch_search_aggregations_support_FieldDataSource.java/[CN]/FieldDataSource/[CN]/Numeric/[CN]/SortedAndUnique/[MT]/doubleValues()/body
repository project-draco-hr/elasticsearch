{
  if (doubleValues == null) {
    doubleValues=delegate.doubleValues();
    if (doubleValues.isMultiValued() && (delegate.getUniqueness() != Uniqueness.UNIQUE || doubleValues.getOrder() != Order.NUMERIC)) {
      doubleValues=new SortedUniqueDoubleValues(doubleValues);
    }
  }
  return doubleValues;
}
