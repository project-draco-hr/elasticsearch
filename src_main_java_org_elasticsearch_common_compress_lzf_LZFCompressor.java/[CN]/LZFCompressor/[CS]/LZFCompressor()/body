{
  this.encoder=ChunkEncoderFactory.safeInstance();
  this.decoder=ChunkDecoderFactory.safeInstance();
  Loggers.getLogger(LZFCompressor.class).debug("using encoder [{}] and decoder[{}] ",this.encoder.getClass().getSimpleName(),this.decoder.getClass().getSimpleName());
}
