{
  super(name);
  this.minSkipCount=postingsFormatSettings.getAsInt("min_skip_count",8);
  this.lowFreqCutoff=postingsFormatSettings.getAsInt("low_freq_cutoff",32);
  this.postingsFormat=new DirectPostingsFormat(minSkipCount,lowFreqCutoff);
}
