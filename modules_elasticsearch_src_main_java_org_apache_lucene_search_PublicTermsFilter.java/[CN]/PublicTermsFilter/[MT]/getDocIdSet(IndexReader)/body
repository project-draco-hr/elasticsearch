{
  OpenBitSet result=new OpenBitSet(reader.maxDoc());
  TermDocs td=reader.termDocs();
  try {
    for (    Term term : terms) {
      td.seek(term);
      while (td.next()) {
        result.fastSet(td.doc());
      }
    }
  }
  finally {
    td.close();
  }
  return result;
}
