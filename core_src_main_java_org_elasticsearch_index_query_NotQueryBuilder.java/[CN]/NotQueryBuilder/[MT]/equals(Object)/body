{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  NotQueryBuilder other=(NotQueryBuilder)obj;
  return Objects.equals(filter,other.filter) && Objects.equals(queryName,other.queryName);
}
