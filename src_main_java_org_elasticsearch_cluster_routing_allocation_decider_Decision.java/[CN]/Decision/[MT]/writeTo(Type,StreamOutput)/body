{
switch (type) {
case NO:
    out.writeVInt(0);
  break;
case YES:
out.writeVInt(1);
break;
case THROTTLE:
out.writeVInt(2);
default :
throw new ElasticsearchIllegalArgumentException("Invalid Type [" + type + "]");
}
}
