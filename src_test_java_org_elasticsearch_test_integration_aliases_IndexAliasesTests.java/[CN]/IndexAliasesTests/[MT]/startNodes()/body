{
  Settings nodeSettings=ImmutableSettings.settingsBuilder().put("action.auto_create_index",false).build();
  startNode("server1",nodeSettings);
  startNode("server2",nodeSettings);
  client1=getClient1();
  client2=getClient2();
  clients=new Client[]{client1,client2};
}
