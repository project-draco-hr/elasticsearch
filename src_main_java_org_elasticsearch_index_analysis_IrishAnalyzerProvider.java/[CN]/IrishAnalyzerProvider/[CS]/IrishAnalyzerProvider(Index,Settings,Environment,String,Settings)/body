{
  super(index,indexSettings,name,settings);
  analyzer=new IrishAnalyzer(version,Analysis.parseStopWords(env,settings,IrishAnalyzer.getDefaultStopSet(),version),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET,version));
}
