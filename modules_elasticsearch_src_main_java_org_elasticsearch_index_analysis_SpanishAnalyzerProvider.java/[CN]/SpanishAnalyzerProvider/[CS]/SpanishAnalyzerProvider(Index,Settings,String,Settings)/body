{
  super(index,indexSettings,name,settings);
  analyzer=new SpanishAnalyzer(version,Analysis.parseStopWords(settings,SpanishAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
