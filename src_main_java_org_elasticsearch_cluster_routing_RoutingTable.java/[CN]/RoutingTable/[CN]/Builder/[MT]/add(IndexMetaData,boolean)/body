{
  if (indexMetaData.state() == IndexMetaData.State.OPEN) {
    IndexRoutingTable.Builder indexRoutingBuilder=new IndexRoutingTable.Builder(indexMetaData.index()).initializeEmpty(indexMetaData,fromApi);
    add(indexRoutingBuilder);
  }
  return this;
}
