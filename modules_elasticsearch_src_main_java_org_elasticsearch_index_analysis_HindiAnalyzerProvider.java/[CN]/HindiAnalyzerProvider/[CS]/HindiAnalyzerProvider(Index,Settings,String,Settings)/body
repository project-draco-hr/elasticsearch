{
  super(index,indexSettings,name,settings);
  analyzer=new HindiAnalyzer(version,Analysis.parseStopWords(settings,HindiAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
