{
  if (channel.getAddress().equals(newView.getCreator())) {
    try {
      channel.disconnect();
    }
 catch (    Exception e) {
    }
    if (!lifecycle.started()) {
      return;
    }
    connectTillMasterIfNeeded();
  }
}
