{
  super(index,indexSettings,name,settings);
  analyzer=new GalicianAnalyzer(version,Analysis.parseStopWords(settings,GalicianAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
