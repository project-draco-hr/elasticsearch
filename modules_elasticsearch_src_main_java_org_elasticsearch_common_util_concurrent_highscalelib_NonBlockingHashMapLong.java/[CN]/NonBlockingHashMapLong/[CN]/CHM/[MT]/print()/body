{
  for (int i=0; i < _keys.length; i++) {
    long K=_keys[i];
    if (K != NO_KEY)     print_impl(i,K,_vals[i]);
  }
  CHM newchm=_newchm;
  if (newchm != null) {
    System.out.println("----");
    newchm.print();
  }
}
