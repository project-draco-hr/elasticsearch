{
  float score=needsScores ? scorer.score() : 0f;
  if (function == null) {
    return subQueryBoost * score;
  }
 else {
    return scoreCombiner.combine(subQueryBoost,score,function.score(scorer.docID(),score),maxBoost);
  }
}
