{
  float score=needsScores ? scorer.score() : 0f;
  if (function == null) {
    return score;
  }
 else {
    return scoreCombiner.combine(score,function.score(scorer.docID(),score),maxBoost);
  }
}
