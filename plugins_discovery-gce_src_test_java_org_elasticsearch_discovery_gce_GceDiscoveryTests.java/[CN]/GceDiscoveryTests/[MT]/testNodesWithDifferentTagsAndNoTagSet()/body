{
  Settings nodeSettings=Settings.builder().put(GceInstancesService.PROJECT_SETTING.getKey(),projectName).put(GceInstancesService.ZONE_SETTING.getKey(),"europe-west1-b").build();
  mock=new GceInstancesServiceMock(nodeSettings);
  List<DiscoveryNode> discoveryNodes=buildDynamicNodes(mock,nodeSettings);
  assertThat(discoveryNodes,hasSize(2));
}
