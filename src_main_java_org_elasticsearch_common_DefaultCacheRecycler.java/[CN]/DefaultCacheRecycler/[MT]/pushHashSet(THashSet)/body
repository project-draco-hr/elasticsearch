{
  Queue<THashSet> ref=hashSet.get();
  if (ref == null) {
    ref=ConcurrentCollections.newQueue();
    hashSet.set(ref);
  }
  if (ref.size() < QUEUE_MAX_SIZE) {
    map.clear();
    ref.add(map);
  }
}
