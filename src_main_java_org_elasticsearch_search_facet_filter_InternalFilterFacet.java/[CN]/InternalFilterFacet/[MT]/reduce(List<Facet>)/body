{
  if (facets.size() == 1) {
    return facets.get(0);
  }
  int count=0;
  for (  Facet facet : facets) {
    count+=((FilterFacet)facet).getCount();
  }
  return new InternalFilterFacet(getName(),count);
}
