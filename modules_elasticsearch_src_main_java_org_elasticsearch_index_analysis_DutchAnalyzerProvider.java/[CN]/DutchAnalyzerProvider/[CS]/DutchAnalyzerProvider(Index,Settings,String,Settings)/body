{
  super(index,indexSettings,name,settings);
  analyzer=new DutchAnalyzer(version,Analysis.parseStopWords(settings,DutchAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
