{
  while (true) {
    Chunk c=curChunk.get();
    if (c != null) {
      return c;
    }
    c=new Chunk(chunkSize);
    if (curChunk.compareAndSet(null,c)) {
      c.init();
      return c;
    }
  }
}
