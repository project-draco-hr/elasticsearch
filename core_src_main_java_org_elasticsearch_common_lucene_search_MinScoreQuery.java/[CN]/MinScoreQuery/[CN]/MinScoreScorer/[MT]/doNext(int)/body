{
  for (; doc != NO_MORE_DOCS; doc=scorer.nextDoc()) {
    final float score=scorer.score();
    if (score >= minScore) {
      this.score=score;
      return doc;
    }
  }
  return NO_MORE_DOCS;
}
