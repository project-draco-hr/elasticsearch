{
  if (commitPending) {
    logger.info("update [{}] from [{}] to [{}]",key,value,pendingValue);
    value=pendingValue;
    consumer.accept(valueInstance);
  }
  commitPending=false;
  valueInstance=null;
  pendingValue=null;
}
