{
  ImmutableSettings.Builder builder=settingsBuilder().put("node.mode","network").put(TransportModule.TRANSPORT_TYPE_KEY,ExceptionThrowingNettyTransport.class.getName());
  return builder.put(super.nodeSettings(nodeOrdinal)).build();
}
