{
  super(index,indexSettings,name,settings);
  analyzer=new IndonesianAnalyzer(version,Analysis.parseStopWords(settings,IndonesianAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
