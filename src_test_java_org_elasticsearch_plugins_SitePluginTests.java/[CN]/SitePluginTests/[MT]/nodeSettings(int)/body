{
  Path pluginDir=getResourcePath("/org/elasticsearch/plugins");
  return settingsBuilder().put(super.nodeSettings(nodeOrdinal)).put("path.plugins",pluginDir.toAbsolutePath()).put("force.http.enabled",true).build();
}
