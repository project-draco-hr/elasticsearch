{
  setupFieldType(context);
  LongFieldMapper fieldMapper=new LongFieldMapper(fieldType,docValues,nullValue,ignoreMalformed(context),coerce(context),fieldDataSettings,context.indexSettings(),multiFieldsBuilder.build(this,context),copyTo);
  fieldMapper.includeInAll(includeInAll);
  return fieldMapper;
}
