{
  super(index,indexSettings,name,settings);
  analyzer=new GermanAnalyzer(version,Analysis.parseStopWords(settings,GermanAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
