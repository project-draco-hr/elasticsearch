{
  if (color(parent) == RED && sibling != NIL && color(sibling) == BLACK && color(left(sibling)) == BLACK && color(right(sibling)) == BLACK) {
    color(sibling,RED);
    color(parent,BLACK);
  }
 else {
    removeCase5(node,parent,sibling);
  }
}
