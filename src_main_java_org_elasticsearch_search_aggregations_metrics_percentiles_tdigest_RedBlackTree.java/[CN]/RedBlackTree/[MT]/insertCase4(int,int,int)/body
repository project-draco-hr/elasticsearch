{
  if (node == right(parent) && parent == left(grandParent)) {
    rotateLeft(parent);
    node=left(node);
  }
 else   if (node == left(parent) && parent == right(grandParent)) {
    rotateRight(parent);
    node=right(node);
  }
  insertCase5(node);
}
