{
  try {
    boolean successful=executor.getQueue().offer(r,waitTime,TimeUnit.MILLISECONDS);
    if (!successful)     throw new EsRejectedExecutionException();
  }
 catch (  InterruptedException e) {
    throw new EsRejectedExecutionException(e);
  }
}
