{
  if (parsedQuery() != null) {
    for (    ScopePhase scopePhase : parsedQuery().scopePhases()) {
      scopePhase.clear();
    }
  }
  try {
    searcher.close();
  }
 catch (  Exception e) {
  }
  engineSearcher.release();
  return true;
}
