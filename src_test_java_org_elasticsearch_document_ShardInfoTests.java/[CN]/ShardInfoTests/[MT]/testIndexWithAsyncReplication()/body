{
  prepareIndex(1);
  IndexResponse indexResponse=client().prepareIndex("idx","type").setReplicationType(ReplicationType.ASYNC).setSource("{}").get();
  assertShardInfo(indexResponse,numCopies,1,numNodes - 1);
}
