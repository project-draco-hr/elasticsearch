{
  final String val2=fieldsFunction.execute(doc,params).toString();
  if (bottom == null) {
    if (val2 == null) {
      return 0;
    }
    return -1;
  }
 else   if (val2 == null) {
    return 1;
  }
  return bottom.compareTo(val2);
}
