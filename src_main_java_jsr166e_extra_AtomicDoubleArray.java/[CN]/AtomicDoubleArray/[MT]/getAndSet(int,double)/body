{
  long next=doubleToRawLongBits(newValue);
  long offset=checkedByteOffset(i);
  while (true) {
    long current=getRaw(offset);
    if (compareAndSetRaw(offset,current,next))     return longBitsToDouble(current);
  }
}
