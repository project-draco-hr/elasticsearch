{
  int[] docOrders=order[docId];
  if (docOrders == null) {
    return null;
  }
  GeoPoint point=valuesCache.get().get();
  int loc=docOrders[0];
  point.latlon(lat[loc],lon[loc]);
  return point;
}
