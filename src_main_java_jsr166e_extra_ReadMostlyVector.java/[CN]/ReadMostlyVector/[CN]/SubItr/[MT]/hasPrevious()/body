{
  boolean valid;
  int i=cursor - 1;
  for (; ; ) {
    if (i >= fence || i < 0 || i >= items.length) {
      valid=false;
      break;
    }
    prev=(E)items[i];
    if (lock.getSequence() == seq) {
      valid=true;
      break;
    }
    refresh();
  }
  return validPrev=valid;
}
