{
  final SequenceLock lock=list.lock;
  lock.lock();
  try {
    if (list.rawRemoveAt(list.rawIndexOf(o,offset,offset + size))) {
      --size;
      return true;
    }
 else     return false;
  }
  finally {
    lock.unlock();
  }
}
