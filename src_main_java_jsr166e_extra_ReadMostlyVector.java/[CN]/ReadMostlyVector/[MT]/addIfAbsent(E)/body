{
  final SequenceLock lock=this.lock;
  lock.lock();
  try {
    if (rawIndexOf(e,0,count) < 0) {
      rawAdd(e);
      return true;
    }
 else     return false;
  }
  finally {
    lock.unlock();
  }
}
