{
  final SequenceLock lock=this.lock;
  lock.lock();
  try {
    Object[] items=array;
    int n=count;
    if (n < items.length)     array=Arrays.copyOf(items,n);
  }
  finally {
    lock.unlock();
  }
}
