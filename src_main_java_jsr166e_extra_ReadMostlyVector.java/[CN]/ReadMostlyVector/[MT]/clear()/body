{
  final SequenceLock lock=this.lock;
  lock.lock();
  try {
    int n=count;
    Object[] items=array;
    for (int i=0; i < n; i++)     items[i]=null;
    count=0;
  }
  finally {
    lock.unlock();
  }
}
