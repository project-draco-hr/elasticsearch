{
  final SequenceLock lock=this.lock;
  boolean ret;
  Object[] elements=c.toArray();
  lock.lock();
  try {
    ret=rawAddAllAt(index,elements);
  }
  finally {
    lock.unlock();
  }
  return ret;
}
