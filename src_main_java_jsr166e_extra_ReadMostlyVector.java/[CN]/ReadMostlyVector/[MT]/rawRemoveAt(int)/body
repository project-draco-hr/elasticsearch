{
  int n=count - 1;
  Object[] items=array;
  if (items == null || index < 0 || index > n)   return false;
  int mv=n - index;
  if (mv > 0)   System.arraycopy(items,index + 1,items,index,mv);
  items[n]=null;
  count=n;
  return true;
}
