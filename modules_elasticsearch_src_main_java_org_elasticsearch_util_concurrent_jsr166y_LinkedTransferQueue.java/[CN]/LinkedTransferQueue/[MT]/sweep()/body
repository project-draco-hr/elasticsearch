{
  for (Node p=head, s, n; p != null && (s=p.next) != null; ) {
    if (p == s)     p=head;
 else     if (!s.isMatched())     p=s;
 else     if ((n=s.next) == null)     break;
 else     p.casNext(s,n);
  }
}
