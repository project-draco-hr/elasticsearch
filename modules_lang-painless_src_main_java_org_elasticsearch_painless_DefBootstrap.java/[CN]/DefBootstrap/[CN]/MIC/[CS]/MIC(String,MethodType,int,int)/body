{
  super(type);
  this.name=name;
  this.flavor=flavor;
  this.flags=flags;
  MethodHandle fallback=FALLBACK.bindTo(this).asCollector(Object[].class,type.parameterCount()).asType(type);
  setTarget(fallback);
}
