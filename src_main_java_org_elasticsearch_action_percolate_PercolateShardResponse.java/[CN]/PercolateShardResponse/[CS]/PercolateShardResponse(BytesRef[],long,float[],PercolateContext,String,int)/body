{
  super(index,shardId);
  this.matches=matches;
  this.count=count;
  this.scores=scores;
  this.percolatorTypeId=context.percolatorTypeId;
  this.requestedSize=context.size;
  buildFacets(context);
}
