{
  try (ReleasableLock lock=writeLock.acquire()){
    if (throwable != null) {
      if (tragedy == null) {
        tragedy=throwable;
      }
 else {
        tragedy.addSuppressed(throwable);
      }
    }
    close();
  }
 }
