{
  try (ReleasableLock lock=writeLock.acquire()){
    if (throwable != null) {
      if (tragicEvent == null) {
        tragicEvent=throwable;
      }
 else {
        tragicEvent.addSuppressed(throwable);
      }
    }
    close();
  }
 }
