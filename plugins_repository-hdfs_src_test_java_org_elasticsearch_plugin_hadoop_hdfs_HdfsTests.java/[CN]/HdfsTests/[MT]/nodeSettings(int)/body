{
  Settings.Builder settings=Settings.builder().put(super.nodeSettings(ordinal)).put("path.home",createTempDir()).put("path.repo","").put(MockFSDirectoryService.RANDOM_PREVENT_DOUBLE_WRITE,false).put(MockFSDirectoryService.RANDOM_NO_DELETE_OPEN_FILE,false);
  return settings.build();
}
