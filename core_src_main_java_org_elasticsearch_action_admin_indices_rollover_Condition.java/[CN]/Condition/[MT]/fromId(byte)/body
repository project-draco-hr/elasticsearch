{
  if (id == 0) {
    return MAX_SIZE;
  }
 else   if (id == 1) {
    return MAX_AGE;
  }
 else   if (id == 2) {
    return MAX_DOCS;
  }
 else {
    throw new IllegalArgumentException("no condition type [" + id + "]");
  }
}
