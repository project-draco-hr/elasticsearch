{
  int s;
  ForkJoinTask<?>[] q=queue;
  int mask=q.length - 1;
  UNSAFE.putOrderedObject(q,(((s=sp++) & mask) << qShift) + qBase,t);
  if ((s-=base) <= 0)   pool.signalWork();
 else   if (s + 1 >= mask)   growQueue();
}
