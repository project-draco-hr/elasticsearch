{
  ForkJoinTask<?> t;
  ForkJoinTask<?>[] q;
  int b, i;
  if ((b=base) != sp && (q=queue) != null && (t=q[i=(q.length - 1) & b]) != null && UNSAFE.compareAndSwapObject(q,(i << qShift) + qBase,t,null)) {
    base=b + 1;
    return t;
  }
  return null;
}
