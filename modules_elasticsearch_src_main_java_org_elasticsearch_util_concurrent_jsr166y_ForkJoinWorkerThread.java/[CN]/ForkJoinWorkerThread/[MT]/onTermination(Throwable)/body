{
  try {
    cancelTasks();
    setTerminated();
    pool.workerTerminated(this);
  }
 catch (  Throwable ex) {
    if (exception == null)     exception=ex;
  }
 finally {
    if (exception != null)     UNSAFE.throwException(exception);
  }
}
