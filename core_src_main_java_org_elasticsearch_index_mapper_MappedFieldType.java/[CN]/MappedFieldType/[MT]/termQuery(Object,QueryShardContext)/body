{
  TermQuery query=new TermQuery(createTerm(value));
  if (boost == 1f || (context != null && context.indexVersionCreated().before(Version.V_5_0_0))) {
    return query;
  }
  return new BoostQuery(query,boost);
}
