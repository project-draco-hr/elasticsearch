{
  super(index,indexSettings,name,settings);
  analyzer=new BulgarianAnalyzer(version,Analysis.parseStopWords(settings,BulgarianAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
