{
  if ("create".equals(opType)) {
    return setOpType(OpType.CREATE);
  }
 else   if ("index".equals(opType)) {
    return setOpType(OpType.INDEX);
  }
 else {
    throw new ElasticSearchIllegalArgumentException("No index opType matching [" + opType + "]");
  }
}
