{
  int start=docBase + prevParentDoc + 1;
  int end=docBase + parentDoc - 1;
  return new ComplexExplanation(true,score(),String.format(Locale.ROOT,"Score based on child doc range from %d to %d",start,end));
}
