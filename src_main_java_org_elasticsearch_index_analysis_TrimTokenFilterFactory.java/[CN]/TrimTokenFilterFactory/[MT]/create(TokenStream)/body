{
  if (version.onOrAfter(Version.LUCENE_4_4_0)) {
    return new TrimFilter(tokenStream);
  }
 else {
    @SuppressWarnings("deprecated") final TokenStream filter=new Lucene43TrimFilter(tokenStream,updateOffsets);
    return filter;
  }
}
