{
  int wc;
  while (runState < TERMINATING && ((wc=workerCounts) >>> TOTAL_COUNT_SHIFT) < parallelism) {
    if ((UNSAFE.compareAndSwapInt(this,workerCountsOffset,wc,wc + (ONE_RUNNING | ONE_TOTAL)) && addWorker() == null))     break;
  }
}
