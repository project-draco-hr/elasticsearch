{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  GsubExpression that=(GsubExpression)o;
  return Objects.equals(fieldName,that.fieldName) && Objects.equals(pattern.pattern(),that.pattern.pattern()) && Objects.equals(replacement,that.replacement);
}
