{
  scriptLang=validateScriptLanguage(scriptLang);
  return client.prepareGet(SCRIPT_INDEX,scriptLang,id).setVersion(version).setVersionType(versionType).setPreference("_local").setOperationThreaded(false).get();
}
