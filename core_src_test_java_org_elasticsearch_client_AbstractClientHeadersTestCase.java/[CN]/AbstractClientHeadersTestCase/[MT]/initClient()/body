{
  Settings settings=Settings.builder().put(HEADER_SETTINGS).put("path.home",createTempDir().toString()).put("name","test-" + getTestName()).build();
  threadPool=new ThreadPool(settings);
  client=buildClient(settings,ACTIONS);
}
