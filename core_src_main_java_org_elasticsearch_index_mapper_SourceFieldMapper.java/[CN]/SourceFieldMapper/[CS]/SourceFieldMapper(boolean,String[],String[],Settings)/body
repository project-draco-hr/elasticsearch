{
  super(NAME,Defaults.FIELD_TYPE.clone(),Defaults.FIELD_TYPE,indexSettings);
  this.enabled=enabled;
  this.includes=includes;
  this.excludes=excludes;
  this.complete=enabled && includes == null && excludes == null;
}
