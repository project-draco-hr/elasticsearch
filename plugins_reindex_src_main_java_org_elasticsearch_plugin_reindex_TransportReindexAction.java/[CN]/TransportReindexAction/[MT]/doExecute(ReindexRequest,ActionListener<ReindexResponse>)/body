{
  validateAgainstAliases(request.getSource(),request.getDestination(),indexNameExpressionResolver,autoCreateIndex,clusterService.state());
  new AsyncIndexBySearchAction(logger,scriptService,client,threadPool,request,listener).start();
}
