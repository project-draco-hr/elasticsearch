{
  if (hunspellDir.exists() && hunspellDir.isDirectory()) {
    for (    File file : hunspellDir.listFiles()) {
      if (file.isDirectory()) {
        if (file.list(AFFIX_FILE_FILTER).length > 0) {
          dictionaries.getUnchecked(file.getName());
        }
      }
    }
  }
}
