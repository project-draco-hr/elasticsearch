{
  Object expression=visit(ctx);
  if (expression instanceof List) {
    @SuppressWarnings("unchecked") List<ALink> links=(List<ALink>)expression;
    return new EChain(line(ctx),offset(ctx),location(ctx),links,false,false,null,null);
  }
 else {
    return expression;
  }
}
