{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  LineStringBuilder other=(LineStringBuilder)obj;
  return Objects.equals(points,other.points) && (translated == other.translated);
}
