{
  super(index,indexSettings,name,settings);
  analyzer=new PortugueseAnalyzer(version,Analysis.parseStopWords(settings,PortugueseAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
