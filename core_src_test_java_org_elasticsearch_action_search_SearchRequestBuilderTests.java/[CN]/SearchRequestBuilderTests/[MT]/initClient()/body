{
  Settings settings=Settings.builder().put(Environment.PATH_HOME_SETTING.getKey(),createTempDir().toString()).put(NetworkModule.TRANSPORT_TYPE_KEY,"local").build();
  client=TransportClient.builder().settings(settings).build();
}
