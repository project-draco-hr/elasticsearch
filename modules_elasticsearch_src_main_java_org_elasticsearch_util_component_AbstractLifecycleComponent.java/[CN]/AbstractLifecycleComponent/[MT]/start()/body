{
  if (!lifecycle.moveToStarted()) {
    return (T)this;
  }
  for (  LifecycleListener listener : listeners) {
    listener.beforeStart();
  }
  doStart();
  for (  LifecycleListener listener : listeners) {
    listener.afterStart();
  }
  return (T)this;
}
