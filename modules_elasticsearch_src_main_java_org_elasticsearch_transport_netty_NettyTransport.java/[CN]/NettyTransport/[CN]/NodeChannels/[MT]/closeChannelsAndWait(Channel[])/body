{
  for (  Channel channel : channels) {
    try {
      if (channel != null && channel.isOpen()) {
        channel.close().awaitUninterruptibly();
      }
    }
 catch (    Exception e) {
    }
  }
}
