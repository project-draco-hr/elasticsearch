{
  double delta=0;
  if (lat < 0) {
    delta=180;
  }
 else   if (lat >= 0) {
    delta=-180;
  }
  double newLat=lat;
  while (newLat <= -90 || newLat >= 90) {
    newLat+=delta;
  }
  return newLat;
}
