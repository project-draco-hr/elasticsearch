{
  Random random=new Random(0);
  while (true) {
    if (close) {
      closed=true;
      return;
    }
    try {
      indexDoc(random);
      Thread.sleep(indexerThrottle.millis());
    }
 catch (    Exception e) {
      logger.warn("failed to index / sleep",e);
    }
  }
}
