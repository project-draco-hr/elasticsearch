{
  OpenBitSet result=new OpenBitSet(reader.maxDoc());
  TermDocs td=reader.termDocs();
  try {
    td.seek(term);
    while (td.next()) {
      result.set(td.doc());
    }
  }
  finally {
    td.close();
  }
  return result;
}
