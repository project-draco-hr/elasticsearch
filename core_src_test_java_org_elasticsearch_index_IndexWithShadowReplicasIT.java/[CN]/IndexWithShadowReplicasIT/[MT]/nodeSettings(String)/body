{
  return Settings.builder().put("node.add_id_to_custom_path",false).put(Environment.PATH_SHARED_DATA_SETTING.getKey(),dataPath).put("index.store.fs.fs_lock",randomFrom("native","simple")).build();
}
