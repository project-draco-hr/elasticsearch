{
  String sSource="_na_";
  try {
    sSource=XContentHelper.convertToJson(script,false);
  }
 catch (  Exception e) {
  }
  return "put script {[" + id + "]["+ scriptLang+ "], script["+ sSource+ "]}";
}
