{
  if (nullValue == null) {
    return null;
  }
  final long value=ipToLong(nullValue);
  return new QueryWrapperFilter(NumericRangeQuery.newLongRange(names.indexName(),precisionStep,value,value,true,true));
}
