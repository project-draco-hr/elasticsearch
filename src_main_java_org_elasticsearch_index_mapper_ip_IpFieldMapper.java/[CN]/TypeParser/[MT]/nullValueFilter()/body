{
  if (nullValue == null) {
    return null;
  }
  final long value=ipToLong(nullValue);
  return Queries.wrap(NumericRangeQuery.newLongRange(names.indexName(),precisionStep,value,value,true,true));
}
