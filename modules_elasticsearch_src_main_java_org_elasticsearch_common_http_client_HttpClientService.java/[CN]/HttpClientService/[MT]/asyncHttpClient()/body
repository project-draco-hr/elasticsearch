{
  if (asyncHttpClient != null) {
    return asyncHttpClient;
  }
synchronized (this) {
    if (asyncHttpClient != null) {
      return asyncHttpClient;
    }
    asyncHttpClient=new AsyncHttpClient();
    return asyncHttpClient;
  }
}
