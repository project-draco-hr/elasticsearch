{
  builder.startObject("hits");
  builder.field("total",totalHits);
  builder.field("hits");
  builder.startArray();
  for (  SearchHit hit : hits) {
    hit.toJson(builder);
  }
  builder.endArray();
  builder.endObject();
}
