{
  totalHits=in.readLong();
  int size=in.readInt();
  if (size == 0) {
    hits=EMPTY;
  }
 else {
    hits=new SearchHit[size];
    for (int i=0; i < hits.length; i++) {
      hits[i]=readSearchHit(in);
    }
  }
}
