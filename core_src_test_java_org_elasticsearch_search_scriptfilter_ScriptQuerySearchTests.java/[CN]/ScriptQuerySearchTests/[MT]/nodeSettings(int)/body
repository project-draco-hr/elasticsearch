{
  return Settings.settingsBuilder().put(super.nodeSettings(nodeOrdinal)).put(FilterCacheModule.FilterCacheSettings.FILTER_CACHE_TYPE,IndexFilterCache.class).put(FilterCacheSettings.FILTER_CACHE_EVERYTHING,true).build();
}
