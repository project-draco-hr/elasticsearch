{
  return Settings.settingsBuilder().put(super.nodeSettings(nodeOrdinal)).put(QueryCacheModule.FilterCacheSettings.FILTER_CACHE_TYPE,IndexQueryCache.class).put(FilterCacheSettings.FILTER_CACHE_EVERYTHING,true).build();
}
