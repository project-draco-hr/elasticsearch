{
  Injector injector=new ModulesBuilder().add(new IndexSettingsModule(ImmutableSettings.Builder.EMPTY_SETTINGS),new IndexNameModule(new Index("test")),new AnalysisModule(ImmutableSettings.Builder.EMPTY_SETTINGS)).createInjector();
  return injector.getInstance(AnalysisService.class);
}
