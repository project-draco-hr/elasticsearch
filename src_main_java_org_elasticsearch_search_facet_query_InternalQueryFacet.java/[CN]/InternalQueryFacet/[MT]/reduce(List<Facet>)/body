{
  if (facets.size() == 1) {
    return facets.get(0);
  }
  int count=0;
  for (  Facet facet : facets) {
    count+=((QueryFacet)facet).getCount();
  }
  return new InternalQueryFacet(getName(),count);
}
