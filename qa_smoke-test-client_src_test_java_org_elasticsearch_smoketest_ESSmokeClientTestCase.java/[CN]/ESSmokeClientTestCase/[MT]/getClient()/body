{
  if (client == null) {
    try {
      client=startClient();
    }
 catch (    UnknownHostException e) {
      logger.error("can not start the client",e);
    }
    assertThat(client,notNullValue());
  }
  return client;
}
