{
  final Action<Entry<K,V>> action;
  if ((action=this.action) != null) {
    for (int b; (b=preSplit()) > 0; )     new ForEachEntryTask<K,V>(map,this,b,action).fork();
    V v;
    while ((v=advance()) != null)     action.apply(entryFor((K)nextKey,v));
    propagateCompletion();
  }
}
