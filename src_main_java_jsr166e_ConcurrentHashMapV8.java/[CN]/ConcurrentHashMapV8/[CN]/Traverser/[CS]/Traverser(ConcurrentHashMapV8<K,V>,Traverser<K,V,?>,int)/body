{
  super(it);
  this.batch=batch;
  if ((this.map=map) != null && it != null) {
    Node<V>[] t;
    if ((t=it.tab) == null && (t=it.tab=map.table) != null)     it.baseLimit=it.baseSize=t.length;
    this.tab=t;
    this.baseSize=it.baseSize;
    int hi=this.baseLimit=it.baseLimit;
    it.baseLimit=this.index=this.baseIndex=(hi + it.baseIndex + 1) >>> 1;
  }
}
