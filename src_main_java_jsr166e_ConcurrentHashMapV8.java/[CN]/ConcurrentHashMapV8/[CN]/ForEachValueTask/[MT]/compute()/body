{
  final Action<V> action;
  if ((action=this.action) != null) {
    for (int b; (b=preSplit()) > 0; )     new ForEachValueTask<K,V>(map,this,b,action).fork();
    V v;
    while ((v=advance()) != null)     action.apply(v);
    propagateCompletion();
  }
}
