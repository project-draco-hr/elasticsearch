{
  Settings settings=settingsBuilder().put("indices.ttl.interval",purgeInterval).put("index.number_of_shards",2).put("cluster.routing.operation.use_type",false).put("cluster.routing.operation.hash.type","djb").build();
  startNode("node1",settings);
  startNode("node2",settings);
  client=getClient();
}
