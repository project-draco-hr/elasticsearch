{
  String[] files=listAll();
  for (  String file : files) {
    deleteFile(file);
  }
  if (!disableCache) {
    ByteBuffer buffer=cache.poll();
    while (buffer != null) {
      closeBuffer(buffer);
      buffer=cache.poll();
    }
  }
}
