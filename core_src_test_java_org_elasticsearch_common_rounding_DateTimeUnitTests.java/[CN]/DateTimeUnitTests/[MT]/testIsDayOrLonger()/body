{
  for (  DateTimeUnit unit : DateTimeUnit.values()) {
    if (DateTimeUnit.isDayOrLonger(unit)) {
      assertTrue(unit == DAY_OF_MONTH || unit == MONTH_OF_YEAR || unit == QUARTER || unit == YEAR_OF_CENTURY || unit == WEEK_OF_WEEKYEAR);
    }
  }
}
