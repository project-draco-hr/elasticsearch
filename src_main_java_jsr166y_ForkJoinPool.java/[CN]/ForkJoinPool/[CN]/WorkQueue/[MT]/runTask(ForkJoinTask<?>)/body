{
  if (t != null) {
    currentSteal=t;
    t.doExec();
    if (top != base) {
      if (mode == 0)       popAndExecAll();
 else       pollAndExecAll();
    }
    ++nsteals;
    currentSteal=null;
  }
}
