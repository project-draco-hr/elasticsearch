{
  for (int i=0; i < times; i++) {
    client().prepareIndex("idx","type").setRouting(shard).setCreate(true).setSource(jsonBuilder().startObject().field("key",key).endObject()).execute().actionGet();
  }
}
