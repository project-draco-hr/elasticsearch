{
  if (size == -1) {
    size=RamUsageEstimator.NUM_BYTES_INT + RamUsageEstimator.NUM_BYTES_INT + RamUsageEstimator.sizeOf(lon)+ RamUsageEstimator.sizeOf(lat)+ RamUsageEstimator.sizeOf(set.getBits());
  }
  return size;
}
