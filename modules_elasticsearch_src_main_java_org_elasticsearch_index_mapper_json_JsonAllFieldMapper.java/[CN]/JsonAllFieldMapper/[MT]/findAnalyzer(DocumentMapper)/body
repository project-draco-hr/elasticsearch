{
  Analyzer analyzer=indexAnalyzer;
  if (analyzer == null) {
    analyzer=docMapper.indexAnalyzer();
    if (analyzer == null) {
      analyzer=Lucene.STANDARD_ANALYZER;
    }
  }
  return analyzer;
}
