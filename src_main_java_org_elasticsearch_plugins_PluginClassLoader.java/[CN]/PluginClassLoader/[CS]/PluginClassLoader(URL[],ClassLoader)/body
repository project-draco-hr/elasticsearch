{
  super(urls,parent);
  url=(urls != null && urls.length > 0 ? urls[0] : null);
  ClassLoader sys=getSystemClassLoader();
  while (sys.getParent() != null) {
    sys=sys.getParent();
  }
  system=sys;
}
