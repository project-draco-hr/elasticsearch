{
  if (isOpen) {
    isOpen=false;
    for (    Directory delegate : distributor.all()) {
      delegate.close();
    }
synchronized (mutex) {
      filesMetadata=ImmutableOpenMap.of();
      files=Strings.EMPTY_ARRAY;
    }
  }
}
