{
  if (size == -1) {
    size=RamUsage.NUM_BYTES_INT + RamUsage.NUM_BYTES_INT + +RamUsage.NUM_BYTES_ARRAY_HEADER+ (values.length * RamUsage.NUM_BYTES_INT)+ ordinals.getMemorySizeInBytes();
  }
  return size;
}
