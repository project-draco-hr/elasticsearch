{
  int count=0;
  File[] files=dir.listFiles();
  if (files != null) {
    for (    File file : files) {
      if (file.isDirectory()) {
        count+=numberOfFiles(file);
      }
 else {
        count++;
      }
    }
  }
  return count;
}
