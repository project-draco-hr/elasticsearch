{
  boolean reverse=false;
  if (name.equals(SortParseElement.SCORE_FIELD_NAME)) {
    if (order == Order.ASC) {
      reverse=true;
    }
  }
 else {
    if (order == Order.DESC) {
      reverse=true;
    }
  }
  return sort(name,reverse);
}
