{
  assertEquals(1,exec("Map settings = ['test1' : '1'];" + "int setting = 0;" + "List keys = ['test0', 'test1', 'test2'];"+ "for (int i = 0; i < keys.size(); ++i) {"+ "    if (settings.containsKey(keys[i])) {"+ "        setting = Integer.parseInt(settings[keys[i]]);"+ "        break;"+ "    }"+ "}"+ "return setting;"));
}
