{
  assertEquals(1,exec("Map settings = ['test1' : '1'];" + "int setting = 0;" + "List keys = ['test0', 'test1', 'test2'];"+ "for (String key : keys) {"+ "    if (settings.containsKey(key)) {"+ "        setting = Integer.parseInt(settings[key]);"+ "        break;"+ "    }"+ "}"+ "return setting;"));
}
