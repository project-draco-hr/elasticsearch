def guess_snapshot(version):
    digits = list(map(int, re.findall('\\d+', version)))
    source = ('%s.%s' % (digits[0], digits[1]))
    destination = ('%s.%s' % (digits[0], (digits[1] + 1)))
    return version.replace(source, destination)
