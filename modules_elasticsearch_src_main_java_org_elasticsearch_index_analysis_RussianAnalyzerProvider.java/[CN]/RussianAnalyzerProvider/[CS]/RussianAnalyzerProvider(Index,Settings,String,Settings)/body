{
  super(index,indexSettings,name,settings);
  analyzer=new RussianAnalyzer(version,Analysis.parseStopWords(settings,RussianAnalyzer.getDefaultStopSet()),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET));
}
