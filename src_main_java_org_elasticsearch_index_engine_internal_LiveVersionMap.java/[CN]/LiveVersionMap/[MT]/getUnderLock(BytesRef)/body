{
  VersionValue value=addsCurrent.get(uid);
  if (value != null) {
    return value;
  }
  value=addsOld.get(uid);
  if (value != null) {
    return value;
  }
  value=deletes.get(uid);
  if (value != null) {
    return value;
  }
  return null;
}
