{
  Version previousMajor;
  do {
    previousMajor=VersionUtils.randomVersion(random());
  }
 while (previousMajor.major != Version.CURRENT.major - 1 && (previousMajor.isAlpha() || previousMajor.isBeta() || previousMajor.isRC()));
  previousMajorVersion=previousMajor;
}
