{
  if (previous == null || next.getType() != PainlessLexer.RBRACK) {
    return false;
  }
switch (previous.getType()) {
case PainlessLexer.RBRACK:
case PainlessLexer.SEMICOLON:
case PainlessLexer.LBRACK:
    return false;
default :
  return true;
}
}
