{
  ForkJoinTask<?>[] a;
  ForkJoinPool p;
  int s=top, n;
  if ((a=array) != null) {
    int m=a.length - 1;
    U.putOrderedObject(a,((m & s) << ASHIFT) + ABASE,task);
    if ((n=(top=s + 1) - base) <= 2)     (p=pool).signalWork(p.workQueues,this);
 else     if (n >= m)     growArray();
  }
}
