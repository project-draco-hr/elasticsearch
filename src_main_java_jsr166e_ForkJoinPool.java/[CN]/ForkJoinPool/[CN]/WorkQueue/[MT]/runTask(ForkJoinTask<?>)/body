{
  if (t != null) {
    (currentSteal=t).doExec();
    currentSteal=null;
    ++nsteals;
    if (base - top < 0) {
      if (mode == 0)       popAndExecAll();
 else       pollAndExecAll();
    }
  }
}
