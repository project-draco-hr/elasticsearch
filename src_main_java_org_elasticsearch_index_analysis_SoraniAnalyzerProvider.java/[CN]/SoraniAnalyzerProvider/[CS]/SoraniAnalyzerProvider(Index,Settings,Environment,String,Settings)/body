{
  super(index,indexSettings,name,settings);
  analyzer=new SoraniAnalyzer(version,Analysis.parseStopWords(env,settings,SoraniAnalyzer.getDefaultStopSet(),version),Analysis.parseStemExclusion(settings,CharArraySet.EMPTY_SET,version));
}
