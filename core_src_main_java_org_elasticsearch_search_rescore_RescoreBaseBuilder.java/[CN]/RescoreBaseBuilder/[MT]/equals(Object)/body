{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  RescoreBaseBuilder other=(RescoreBaseBuilder)obj;
  return Objects.equals(windowSize,other.windowSize) && Objects.equals(rescorer,other.rescorer);
}
