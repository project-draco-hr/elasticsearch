{
  int ceiling=NIL;
  for (int node=root(); node != NIL; ) {
    final int cmp=Double.compare(mean(node),centroid);
    if (cmp < 0) {
      node=right(node);
    }
 else {
      ceiling=node;
      node=left(node);
    }
  }
  return ceiling;
}
