{
  int floor=NIL;
  for (int node=root(); node != NIL; ) {
    final int cmp=Double.compare(centroid,mean(node));
    if (cmp <= 0) {
      node=left(node);
    }
 else {
      floor=node;
      node=right(node);
    }
  }
  return floor;
}
