{
  if (lock != null)   return true;
  if (!path.exists())   return false;
  try {
    boolean obtained=obtain();
    if (obtained)     close();
    return !obtained;
  }
 catch (  IOException ioe) {
    return false;
  }
}
