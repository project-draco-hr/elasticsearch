{
  if (ITER++ == 0) {
    for (int i=0; i < SEQUENCE.length; i++) {
      SEQUENCE[i]=randomLong();
    }
  }
 else {
    for (int i=0; i < SEQUENCE.length; i++) {
      assertThat(SEQUENCE[i],equalTo(randomLong()));
    }
  }
}
