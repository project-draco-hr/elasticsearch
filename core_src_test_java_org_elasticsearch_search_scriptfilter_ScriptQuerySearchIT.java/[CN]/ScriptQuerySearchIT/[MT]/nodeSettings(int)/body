{
  return Settings.settingsBuilder().put(super.nodeSettings(nodeOrdinal)).put(QueryCacheModule.QueryCacheSettings.QUERY_CACHE_TYPE,IndexQueryCache.class).put(QueryCacheSettings.QUERY_CACHE_EVERYTHING,true).build();
}
