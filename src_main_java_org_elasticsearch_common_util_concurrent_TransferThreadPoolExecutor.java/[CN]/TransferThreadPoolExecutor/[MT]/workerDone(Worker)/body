{
  final ReentrantLock mainLock=this.mainLock;
  mainLock.lock();
  try {
    workers.remove(w);
    if (poolSize.decrementAndGet() == 0)     tryTerminate();
  }
  finally {
    mainLock.unlock();
  }
}
