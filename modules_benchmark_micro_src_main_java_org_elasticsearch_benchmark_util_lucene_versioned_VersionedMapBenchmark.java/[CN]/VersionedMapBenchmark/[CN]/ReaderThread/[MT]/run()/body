{
  try {
    barrier1.await();
    barrier2.await();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  for (int i=0; i < readerIterations; i++) {
    versionedMap.beforeVersion(i,i);
  }
  latch.countDown();
}
