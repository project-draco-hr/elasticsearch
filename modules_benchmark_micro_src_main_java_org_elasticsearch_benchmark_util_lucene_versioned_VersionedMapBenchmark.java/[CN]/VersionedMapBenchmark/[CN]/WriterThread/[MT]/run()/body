{
  try {
    barrier1.await();
    barrier2.await();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  for (int i=0; i < writerIterations; i++) {
    versionedMap.putVersionIfAbsent(i,i);
  }
  latch.countDown();
}
