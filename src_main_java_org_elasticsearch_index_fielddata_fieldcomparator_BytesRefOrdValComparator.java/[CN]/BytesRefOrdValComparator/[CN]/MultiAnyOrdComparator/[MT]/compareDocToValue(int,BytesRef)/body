{
  BytesRef docValue=getRelevantValue(termsIndex,doc,reversed);
  if (docValue == null) {
    if (value == null) {
      return 0;
    }
    return -1;
  }
 else   if (value == null) {
    return 1;
  }
  return docValue.compareTo(value);
}
