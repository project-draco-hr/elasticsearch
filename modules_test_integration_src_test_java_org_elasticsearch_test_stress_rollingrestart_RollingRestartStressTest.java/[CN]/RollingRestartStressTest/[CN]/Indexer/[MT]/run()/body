{
  while (true) {
    if (closed) {
      return;
    }
    try {
      indexDoc();
      Thread.sleep(indexerThrottle.millis());
    }
 catch (    IOException e) {
      logger.warn("failed to index",e);
    }
catch (    InterruptedException e) {
      break;
    }
  }
}
