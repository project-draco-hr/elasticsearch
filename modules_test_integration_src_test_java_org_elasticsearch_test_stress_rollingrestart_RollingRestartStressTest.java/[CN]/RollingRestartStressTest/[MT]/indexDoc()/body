{
  long id=indexCounter.incrementAndGet();
  client.client().prepareIndex("test","type1").setSource(XContentFactory.jsonBuilder().startObject().field("field","value" + id).endObject()).execute().actionGet();
}
