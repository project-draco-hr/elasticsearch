{
  double delta=0;
  if (lon < 0) {
    delta=360;
  }
 else   if (lon >= 0) {
    delta=-360;
  }
  double newLng=lon;
  while (newLng < -180 || newLng > 180) {
    newLng+=delta;
  }
  return newLng;
}
