{
  CountRequestBuilder countRequestBuilder=client.prepareCount();
  assertThat(countRequestBuilder.toString(),equalTo(new SearchSourceBuilder().size(0).minScore(CountRequest.DEFAULT_MIN_SCORE).terminateAfter(SearchContext.DEFAULT_TERMINATE_AFTER).toString()));
}
