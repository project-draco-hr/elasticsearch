{
  super(indexSettings,name,settings);
  this.separator=parseSeparator(settings);
  this.maxOutputSize=settings.getAsInt(MAX_OUTPUT_SIZE.getPreferredName(),FingerprintTokenFilterFactory.DEFAULT_MAX_OUTPUT_SIZE);
}
