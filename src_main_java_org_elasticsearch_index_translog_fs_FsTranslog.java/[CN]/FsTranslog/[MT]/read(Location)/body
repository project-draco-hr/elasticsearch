{
  rwl.readLock().lock();
  try {
    FsTranslogFile trans=translogForLocation(location);
    if (trans != null) {
      try {
        return trans.read(location);
      }
 catch (      Exception e) {
      }
    }
    return null;
  }
  finally {
    rwl.readLock().unlock();
  }
}
