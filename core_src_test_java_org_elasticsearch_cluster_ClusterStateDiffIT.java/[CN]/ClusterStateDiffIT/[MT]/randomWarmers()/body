{
  if (randomBoolean()) {
    return new IndexWarmersMetaData(new IndexWarmersMetaData.Entry(randomName("warm"),new String[]{randomName("type")},randomBoolean(),new SearchSourceBuilder()));
  }
 else {
    return new IndexWarmersMetaData();
  }
}
