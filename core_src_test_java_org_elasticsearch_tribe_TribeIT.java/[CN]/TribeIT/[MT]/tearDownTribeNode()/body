{
  if (cluster2 != null) {
    try {
      cluster2.wipe(Collections.<String>emptySet());
    }
  finally {
      cluster2.afterTest();
    }
  }
  if (tribeNode != null) {
    tribeNode.close();
    tribeNode=null;
  }
}
