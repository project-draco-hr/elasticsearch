{
  if (cluster2 == null) {
    final NodeConfigurationSource configSource=getNodeConfigSource();
    cluster2=new InternalTestCluster(randomLong(),createTempDir(),true,2,2,UUIDs.randomBase64UUID(random()),configSource,0,false,SECOND_CLUSTER_NODE_PREFIX,getMockPlugins(),Function.identity());
    cluster2.beforeTest(random(),0.1);
    cluster2.ensureAtLeastNumDataNodes(2);
  }
}
