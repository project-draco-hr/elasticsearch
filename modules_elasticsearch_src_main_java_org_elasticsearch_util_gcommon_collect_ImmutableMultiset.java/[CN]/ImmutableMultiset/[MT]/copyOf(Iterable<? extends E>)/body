{
  if (elements instanceof ImmutableMultiset) {
    @SuppressWarnings("unchecked") ImmutableMultiset<E> result=(ImmutableMultiset<E>)elements;
    return result;
  }
  @SuppressWarnings("unchecked") Multiset<? extends E> multiset=(elements instanceof Multiset) ? (Multiset<? extends E>)elements : LinkedHashMultiset.create(elements);
  return copyOfInternal(multiset);
}
