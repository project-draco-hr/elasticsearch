{
  boolean success=false;
  try {
    recoverFromTranslog(engineConfig.getTranslogRecoveryPerformer());
    success=true;
  }
  finally {
    if (success == false) {
      close();
    }
  }
  return this;
}
