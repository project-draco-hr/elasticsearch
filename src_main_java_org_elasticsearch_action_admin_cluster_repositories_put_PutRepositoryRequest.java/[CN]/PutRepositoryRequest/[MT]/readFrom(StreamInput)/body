{
  super.readFrom(in);
  name=in.readString();
  type=in.readString();
  settings=readSettingsFromStream(in);
  readTimeout(in);
  if (in.getVersion().onOrAfter(Version.V_1_4_0)) {
    verify=in.readBoolean();
  }
 else {
    verify=false;
  }
}
