{
  super.readFrom(in);
  type=in.readUTF();
  if (in.readBoolean()) {
    id=in.readUTF();
  }
  source=new byte[in.readInt()];
  in.readFully(source,0,source.length);
  opType=OpType.fromId(in.readByte());
}
