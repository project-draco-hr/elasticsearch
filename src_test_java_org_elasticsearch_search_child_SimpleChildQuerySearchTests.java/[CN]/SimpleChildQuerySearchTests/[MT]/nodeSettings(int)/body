{
  return ImmutableSettings.settingsBuilder().put(super.nodeSettings(nodeOrdinal)).put(FilterCacheModule.FilterCacheSettings.FILTER_CACHE_TYPE,WeightedFilterCache.class).put(AutoFilterCachingPolicy.AGGRESSIVE_CACHING_SETTINGS).build();
}
