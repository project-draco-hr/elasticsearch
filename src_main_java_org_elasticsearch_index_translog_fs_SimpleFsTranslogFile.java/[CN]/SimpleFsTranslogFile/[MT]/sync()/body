{
  if (!syncNeeded()) {
    return;
  }
  rwl.writeLock().lock();
  try {
    lastSyncPosition=lastWrittenPosition;
    channelReference.channel().force(false);
  }
  finally {
    rwl.writeLock().unlock();
  }
}
