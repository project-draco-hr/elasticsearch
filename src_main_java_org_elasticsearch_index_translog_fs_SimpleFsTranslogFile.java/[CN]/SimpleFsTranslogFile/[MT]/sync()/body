{
  if (!syncNeeded()) {
    return;
  }
  rwl.writeLock().lock();
  try {
    lastSyncPosition=lastWrittenPosition;
    raf.channel().force(false);
  }
  finally {
    rwl.writeLock().unlock();
  }
}
