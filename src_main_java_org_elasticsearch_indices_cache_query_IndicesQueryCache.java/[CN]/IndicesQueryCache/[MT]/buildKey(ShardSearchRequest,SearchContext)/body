{
  BytesStreamOutput out=new BytesStreamOutput();
  request.writeTo(out,true);
  return new Key(context.indexShard(),((DirectoryReader)context.searcher().getIndexReader()).getVersion(),out.bytes().copyBytesArray());
}
