{
  verifyStarted();
  if (logger.isTraceEnabled()) {
    logger.trace("optimize with {}",optimize);
  }
  engineSafe().forceMerge(optimize.flush(),optimize.waitForMerge(),optimize.maxNumSegments(),optimize.onlyExpungeDeletes(),optimize.upgrade());
}
