{
  List<Object> list;
  if (maybeList instanceof List) {
    list=(List<Object>)maybeList;
  }
 else {
    list=new ArrayList<>();
    list.add(maybeList);
  }
  appendValues(list,value);
  return list;
}
