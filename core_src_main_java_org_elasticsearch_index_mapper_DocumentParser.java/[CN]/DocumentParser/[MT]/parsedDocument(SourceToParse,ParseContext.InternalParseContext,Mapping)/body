{
  return new ParsedDocument(context.version(),context.sourceToParse().id(),context.sourceToParse().type(),source.routing(),source.timestamp(),source.ttl(),context.docs(),context.sourceToParse().source(),update).parent(source.parent());
}
