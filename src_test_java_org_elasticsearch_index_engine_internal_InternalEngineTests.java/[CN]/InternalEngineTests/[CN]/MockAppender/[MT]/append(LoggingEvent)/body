{
  if (event.getLevel() == Level.TRACE && event.getLoggerName().equals("lucene.iw") && event.getMessage().toString().contains("IW: apply all deletes during flush") && event.getMessage().toString().contains("[index][1] ")) {
    sawIndexWriterMessage=true;
  }
}
