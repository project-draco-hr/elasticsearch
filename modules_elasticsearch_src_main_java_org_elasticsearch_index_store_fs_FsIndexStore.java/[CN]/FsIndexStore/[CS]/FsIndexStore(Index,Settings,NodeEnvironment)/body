{
  super(index,indexSettings);
  this.location=new File(new File(nodeEnv.nodeFile(),"indices"),index.name());
  if (!location.exists()) {
    for (int i=0; i < 5; i++) {
      if (location.mkdirs()) {
        break;
      }
    }
  }
}
