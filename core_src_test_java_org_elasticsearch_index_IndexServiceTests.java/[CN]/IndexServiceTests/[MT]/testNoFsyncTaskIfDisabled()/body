{
  Settings settings=Settings.builder().put(IndexSettings.INDEX_TRANSLOG_SYNC_INTERVAL,"0ms").build();
  IndexService indexService=createIndex("test",settings);
  assertNull(indexService.getFsyncTask());
}
