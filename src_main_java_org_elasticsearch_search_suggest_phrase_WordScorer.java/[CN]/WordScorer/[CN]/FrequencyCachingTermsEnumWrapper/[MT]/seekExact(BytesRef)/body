{
  last=cache.get(text);
  if (last != null) {
    return last.ttf != 0 || last.df != 0;
  }
  last=new CacheEntry();
  cache.put(BytesRef.deepCopyOf(text),last);
  if (in.seekExact(text)) {
    last.df=-1;
    last.ttf=-1;
    return true;
  }
  return false;
}
