{
  if (rescorer != null) {
    builder.startObject("rescore");
    if (windowSize != null) {
      builder.field("window_size",windowSize);
    }
    rescorer.toXContent(builder,params);
    builder.endObject();
  }
  return builder;
}
