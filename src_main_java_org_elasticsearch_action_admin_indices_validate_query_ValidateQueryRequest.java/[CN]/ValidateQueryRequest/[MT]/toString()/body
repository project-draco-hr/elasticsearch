{
  String sSource="_na_";
  try {
    sSource=XContentHelper.convertToJson(source,false);
  }
 catch (  Exception e) {
  }
  return "[" + Arrays.toString(indices) + "]"+ Arrays.toString(types)+ ", source["+ sSource+ "], explain:"+ explain+ ", rewrite:"+ rewrite;
}
