{
  for (int i=0; i < internalCluster().size(); i++) {
    internalCluster().stopRandomDataNode();
  }
  httpsServer.stop(0);
  httpServer.stop(0);
  httpsServer=null;
  httpServer=null;
  logDir=null;
}
