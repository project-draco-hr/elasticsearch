{
  if (refCount.decrementAndGet() <= 0) {
    try {
      raf.close();
      if (delete) {
        file.delete();
      }
 else {
        FileSystemUtils.syncFile(file);
      }
    }
 catch (    IOException e) {
    }
  }
}
