{
  if (randomBoolean()) {
    logger.info("using default jsonp settings (should be false)");
    return super.nodeSettings(nodeOrdinal);
  }
  return ImmutableSettings.settingsBuilder().put(RestController.HTTP_JSON_ENABLE,false).put(super.nodeSettings(nodeOrdinal)).build();
}
