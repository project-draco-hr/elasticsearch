{
  if (randomBoolean()) {
    logger.info("using default jsonp settings (should be false)");
    return super.nodeSettings(nodeOrdinal);
  }
  return ImmutableSettings.settingsBuilder().put(super.nodeSettings(nodeOrdinal)).put(RestController.HTTP_JSON_ENABLE,false).build();
}
