{
  Preconditions.checkNotNull(rootObjectMapper,"Mapper builder must have the root object mapper set");
  return new DocumentMapper(index,docMapperParser,rootObjectMapper,meta,indexAnalyzer,searchAnalyzer,rootMappers);
}
