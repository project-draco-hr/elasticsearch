{
  if (value == null || value.length() == 0) {
    return Values.FALSE;
  }
  if (value.length() == 1 && value.charAt(0) == 'F') {
    return Values.FALSE;
  }
  if (Booleans.parseBoolean(value,false)) {
    return Values.TRUE;
  }
  return Values.FALSE;
}
