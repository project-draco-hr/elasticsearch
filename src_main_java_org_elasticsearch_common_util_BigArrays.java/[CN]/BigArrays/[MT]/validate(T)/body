{
  boolean success=false;
  try {
    validate(array.sizeInBytes());
    success=true;
  }
  finally {
    if (!success) {
      Releasables.closeWhileHandlingException(array);
    }
  }
  return array;
}
