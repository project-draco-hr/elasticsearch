{
  boolean success=false;
  try {
    validate(array.ramBytesUsed());
    success=true;
  }
  finally {
    if (!success) {
      Releasables.closeWhileHandlingException(array);
    }
  }
  return array;
}
