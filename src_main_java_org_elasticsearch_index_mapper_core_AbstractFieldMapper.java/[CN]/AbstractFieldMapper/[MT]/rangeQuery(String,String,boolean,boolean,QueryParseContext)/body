{
  return new TermRangeQuery(names.indexName(),lowerTerm == null ? null : new BytesRef(indexedValue(lowerTerm)),upperTerm == null ? null : new BytesRef(indexedValue(upperTerm)),includeLower,includeUpper);
}
