{
  if (valueLoaded) {
    return value;
  }
  valueLoaded=true;
  value=null;
  IndexableField field=doc.getField(mapper.names().indexName());
  if (field == null) {
    return null;
  }
  value=mapper.value((Field)field);
  return value;
}
