{
  if (pendingOff > 0) {
    if (values.length == valuesOff) {
      grow(valuesOff + 1);
    }
    packPendingValues();
    valuesBytes+=values[valuesOff].ramBytesUsed();
    ++valuesOff;
    pendingOff=0;
  }
  pending=null;
}
