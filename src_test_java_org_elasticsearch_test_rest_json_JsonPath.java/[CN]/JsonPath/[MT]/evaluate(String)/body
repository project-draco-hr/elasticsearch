{
  String[] parts=parsePath(path);
  Object object=jsonMap;
  for (  String part : parts) {
    object=evaluate(part,object);
    if (object == null) {
      return null;
    }
  }
  return object;
}
